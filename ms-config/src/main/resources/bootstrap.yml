server:
  port: ${SERVER_PORT:8888}

spring:
  application:
    name: ms-config

management:
  server:
    port: ${SERVER_PORT:9999}
  metrics:
    enable:
      all: true
      jvm: true
  endpoints:
    web:
      exposure:
        include: '*'
    health:
      show-details: ALWAYS


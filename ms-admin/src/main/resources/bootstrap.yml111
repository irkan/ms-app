server:
  port: ${SERVER_PORT:8508}
spring:
  profiles:
    active: ${PROFILE:DEV}
  application:
    name: ms-admin

  cloud:
    config:
      profile: native
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}